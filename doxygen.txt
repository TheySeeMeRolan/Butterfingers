/// single line 

/** 
    multi-line code
*/

///Class tags
@file - only necessary when defining globals (specify to which class it belongs)
@class - used in conjuction with brief to add class to list of classes (always neccessary)
@author
@version
@brief - brief description as neccessary of the class

///Function tags
@param
@return
@todo

https://www.rosettacommons.org/docs/latest/development_documentation/tutorials/doxygen-tips

Example from doxygen:

/**
 *  A test class. A more elaborate class description.
 */
 
class doc_Test
{
  public:
 
    /** 
     * An enum.
     * More detailed enum description.
     */
 
    enum TEnum { 
          TVal1, /**< enum value TVal1. */  
          TVal2, /**< enum value TVal2. */  
          TVal3  /**< enum value TVal3. */  
         } 
       *enumPtr, /**< enum pointer. Details. */
       enumVar;  /**< enum variable. Details. */
       
      /**
       * A constructor.
       * A more elaborate description of the constructor.
       */
      doc_Test();
 
      /**
       * A destructor.
       * A more elaborate description of the destructor.
       */
     ~doc_Test();
    
      /**
       * a normal member taking two arguments and returning an integer value.
       * @param a an integer argument.
       * @param s a constant character pointer.
       * @see doc_Test()
       * @see doc_Test()
       * @see testMeToo()
       * @see publicVar()
       * @return The test results
       */
       int testMe(int a,const char *s);
       
      /**
       * A pure virtual member.
       * @see testMe()
       * @param c1 the first argument.
       * @param c2 the second argument.
       */
       virtual void testMeToo(char c1,char c2) = 0;
   
      /** 
       * a public variable.
       * Details.
       */
       int publicVar;
       
      /**
       * a function variable.
       * Details.
       */
       int (*handler)(int a,int b);
};